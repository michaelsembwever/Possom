<?xml version="1.0" encoding="UTF-8"?>
<!--
 - Copyright (2005-2006) Schibsted Søk AS
 - $Id: project.xml 2296 2006-02-14 17:33:59Z magnuse $
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>sesat</groupId>
        <artifactId>pom.generic.sesam.no</artifactId>
        <version>2.17-SNAPSHOT</version>
    </parent>

    <name>${project.artifactId}</name>
    <artifactId>generic.sesam.no</artifactId>
    <packaging>war</packaging>
    <description>Generic Search-config Skin.</description>
    
    <profiles>
        <!-- DEVELOPMENT PROFILE -->
        <profile>
            <id>development</id>
            <activation><activeByDefault>true</activeByDefault></activation>
            <build><plugins><plugin>
                <groupId>sesat</groupId>
                <artifactId>sesat-mojo</artifactId>
                <executions><execution>
                    <id>copy-self</id>
                    <phase>install</phase>
                    <goals><goal>deploy</goal></goals>
                </execution></executions>
            </plugin></plugins></build>
        </profile>
        <profile>
            <id>include-fast</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-war-plugin</artifactId>
                        <configuration><classifier>include-fast</classifier></configuration>
                    </plugin>
                    <plugin>
                        <groupId>sesat</groupId>
                        <artifactId>sesat-mojo</artifactId>
                        <executions><execution>
                            <id>copy-self</id>
                            <phase>install</phase>
                            <goals><goal>deploy</goal></goals>
                        </execution></executions>
                    </plugin>
                </plugins>
            </build>
            <dependencies>
                <dependency><!-- override making optional -->
                    <groupId>sesat</groupId>
                    <artifactId>generic.sesam.no-search-command-control</artifactId>
                    <version>${pom.version}</version>
                    <optional>true</optional>
                </dependency>
                <dependency><!-- add the include-fast variant -->
                    <groupId>sesat</groupId>
                    <artifactId>generic.sesam.no-search-command-control</artifactId>
                    <version>${pom.version}</version>
                    <classifier>include-fast</classifier>
                </dependency>                
            </dependencies>
        </profile>
        <profile><id>alpha</id>
        <!-- redefine sesat-kernel's environment classifiers as the include-fast profile would otherwise clobber it!
           -  this means that there's no way of determining if an alpha build includes fast or not. 
           - This headache would be solved if http://jira.codehaus.org/browse/MNG-2596 was implemented. -->
            <build>
                <plugins>
                  <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <configuration><classifier>alpha</classifier></configuration>
                  </plugin>
                </plugins>
            </build>
        </profile>
        <profile><id>nuclei</id>
        <!-- redefine sesat-kernel's environment classifiers as the include-fast profile would otherwise clobber it!
           -  this means that there's no way of determining if an alpha build includes fast or not. 
           - This headache would be solved if http://jira.codehaus.org/browse/MNG-2596 was implemented. -->
            <build>
                <plugins>
                  <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <configuration><classifier>nuclei</classifier></configuration>
                  </plugin>
                </plugins>
            </build>
        </profile>
        <profile><id>beta</id>
        <!-- redefine sesat-kernel's environment classifiers as the include-fast profile would otherwise clobber it!
           -  this means that there's no way of determining if an alpha build includes fast or not. 
           - This headache would be solved if http://jira.codehaus.org/browse/MNG-2596 was implemented. -->
            <build>
                <plugins>
                  <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <configuration><classifier>beta</classifier></configuration>
                  </plugin>
                </plugins>
            </build>
        </profile>
        <profile><id>electron</id>
        <!-- redefine sesat-kernel's environment classifiers as the include-fast profile would otherwise clobber it!
           -  this means that there's no way of determining if an alpha build includes fast or not. 
           - This headache would be solved if http://jira.codehaus.org/browse/MNG-2596 was implemented. -->
            <build>
                <plugins>
                  <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <configuration><classifier>electron</classifier></configuration>
                  </plugin>
                </plugins>
            </build>
        </profile>
        <profile><id>gamma</id>
        <!-- redefine sesat-kernel's environment classifiers as the include-fast profile would otherwise clobber it!
           -  this means that there's no way of determining if an alpha build includes fast or not. 
           - This headache would be solved if http://jira.codehaus.org/browse/MNG-2596 was implemented. -->
            <build>
                <plugins>
                  <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <configuration><classifier>gamma</classifier></configuration>
                  </plugin>
                </plugins>
            </build>
        </profile>
        <profile><id>production</id>
        <!-- redefine sesat-kernel's environment classifiers as the include-fast profile would otherwise clobber it!
           -  this means that there's no way of determining if an alpha build includes fast or not. 
           - This headache would be solved if http://jira.codehaus.org/browse/MNG-2596 was implemented. -->
            <build>
                <plugins>
                  <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <configuration><classifier>production</classifier></configuration>
                  </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    
    <dependencies>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>sesat-resourcefeed</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>commons-log4j</artifactId>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>generic.sesam.no-query-transform-config</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>generic.sesam.no-query-transform-control</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>generic.sesam.no-result</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>generic.sesam.no-result-handler-config</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>generic.sesam.no-result-handler-control</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>generic.sesam.no-search-command-config</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>generic.sesam.no-search-command-control</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>generic.sesam.no-view-config</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>generic.sesam.no-view-control</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>sesat</groupId>
            <artifactId>generic.sesam.no-velocity-directives</artifactId>
            <version>${pom.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>sesat</groupId>
                <artifactId>sesat-mojo</artifactId>
                <version>${pom.version}</version>
                <configuration>
                    <outputDir>
                        src/main/conf/
                    </outputDir>
                    <classpaths>
                        <classpath>../query-transform-config/src/main/java/</classpath>
                        <classpath>../search-command-config/src/main/java/</classpath>
                        <classpath>../result-handler-config/src/main/java/</classpath>
                   
                        <classpath>../../query-transform-config-spi/src/main/java/</classpath>
                        <classpath>../../search-command-config-spi/src/main/java/</classpath>
                        <classpath>../../result-handler-config-spi/src/main/java/</classpath>
                    </classpaths>
                </configuration>
                <executions>
                    <execution>
                        <phase>compile</phase>
                        <goals>
                            <goal>searchModesSchemaGenerator</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
